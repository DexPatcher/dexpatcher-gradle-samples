task build {
    dependsOn gradle.includedBuild('patched-app').task(':app:build')
    dependsOn gradle.includedBuild('multi-dex').task(':app:build')
    dependsOn gradle.includedBuild('apk-lib').task(':app:build')
    //dependsOn gradle.includedBuild('patch-lib').task(':app:build')
    //dependsOn gradle.includedBuild('patched-dex').task(':build')
}

task clean {
    dependsOn gradle.includedBuild('patched-app').task(':app:clean')
    dependsOn gradle.includedBuild('multi-dex').task(':app:clean')
    dependsOn gradle.includedBuild('apk-lib').task(':apklib:build')
    dependsOn gradle.includedBuild('apk-lib').task(':app:build')
    //dependsOn gradle.includedBuild('patch-lib').task(':patch:clean')
    //dependsOn gradle.includedBuild('patch-lib').task(':app:clean')
    //dependsOn gradle.includedBuild('patched-dex').task(':clean')
}
